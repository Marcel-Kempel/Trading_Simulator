{
  "name": "trading-simulator-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "api:dev": "npm --prefix backend run dev",
    "api:test": "npm --prefix backend run test"
  }
}
